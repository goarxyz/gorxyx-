# goarxyz Hub Environment Variables
# The app functions entirely offline/in-memory with public endpoints, so no keys are strictly required.

# Port (configured automatically by AI Studio)
# PORT=3000
